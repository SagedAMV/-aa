# واجهات تطبيق مخزن الأدوات - دليل المستخدم الكامل

## نظرة عامة على التنقل
التطبيق يستخدم نظام تبويب سفلي (Bottom Tabs) + شاشات Stack علوية.

---

### 1. شاشة تسجيل الدخول `login.tsx`
**المسار:** `/login`
**ما يظهر للمستخدم:**
- شعار التطبيق (icon.png)
- عنوان: "نظام إدارة مخزن الأدوات"
- وصف: "يعمل بالكامل دون إنترنت — قاعدة بيانات محلية"
- حقلين:
  - اسم المستخدم (افتراضي: admin)
  - كلمة المرور (secure)
- زر "تسجيل الدخول" مع أيقونة log-in
- ملاحظة صفراء: الحساب الافتراضي admin / admin123
- **المنطق:** يتحقق من Firestore، في حال عدم وجود مستخدمين يعمل بـ admin المحلي. يحفظ الجلسة في AsyncStorage.

### 2. التبويب الرئيسي - الرئيسية `home.tsx`
**المسار:** `/(tabs)/home`
**ما يظهر:**
- رأس الترحيب: "مرحباً، [الاسم]" + الدور + أيقونة cube
- **تنبيهات ملونة (إن وجدت):**
  - أحمر: "X أداة تجاوزت موعد الإرجاع" -> يذهب لفلتر overdue
  - أزرق: "X أداة بكمية منخفضة" -> يذهب لفلتر low
- **بطاقات إحصائية (Stat Cards):**
  - عدد الأصناف
  - (يمكن توسيعها: إجمالي الوحدات، المتاح، المسحوب...)
- **إجراءات سريعة (6 أزرار شبكة 3×2):**
  - مسح باركود (qr-code)
  - تسجيل صرف (arrow-down-circle)
  - تسجيل إضافة (add-circle)
  - أداة جديدة (cube)
  - التقارير (document-text)
  - استيراد Excel (cloud-upload)
- تذييل: "📴 جميع البيانات مخزنة على هذا الجهاز فقط"

### 3. تبويب الأدوات `tools.tsx`
**المسار:** `/(tabs)/tools`
**ما يظهر:**
- شريط بحث علوي مع أيقونة search + زر مسح + زر QR للمسح السريع
- شريط فلاتر أفقي (chips): الكل + قائمة التصنيفات الملونة (يضيء المختار بلونه)
- ملخص: "X صنف — Y وحدة" + زر toggle "كمية منخفضة" (يصبح أحمر عند التفعيل)
- قائمة كروت عمودية، كل كارت:
  - شريط جانبي ملون بلون التصنيف
  - اسم الأداة + Badge "منخفضة" أحمر إن applicable
  - صف Meta: تصنيف (pricetag), موقع (location), تسلسلي (barcode)
  - 3 حبات: المتاح (أخضر) / مسحوب (برتقالي) / الإجمالي (أزرق)
  - سهم chevron-back
  - عند الضغط -> تفاصيل الأداة
- FAB زر عائم + لإضافة أداة

### 4. جدول الأصناف المفصل `tools_table.tsx`
**المسار:** `/tools_table`
**ما يظهر:**
- حقل بحث
- صف فرز: فرز بالاسم / بالكمية
- Header جدول: الصنف | متاح/كلي | الموقع | التصنيف (اختياري حسب الإعدادات)
- قائمة صفوف قابلة للضغط، تذهب للتفاصيل
- FAB إضافة

### 5. تفاصيل الأداة `tool/[id].tsx`
**المسار:** `/tool/:id`
**ما يظهر:**
- كارت رأس: صورة الأداة (أو placeholder) + اسم + Badges (تصنيف، منخفضة، غير متاح)
- شبكة 3 أرقام: الإجمالي / المتاح / مسحوب
- زرين أفقيين: "صرف" (معطل إذا المتاح 0) + "إضافة كمية" (أخضر)
- قسم "التفاصيل" (Card):
  - InfoRow: الرقم التسلسلي, الباركود, الموقع, حد التنبيه, آخر تحديث
  - وصف + ملاحظات إن وجدت
- قسم "سجل الصرف (X)": آخر 20 عملية، كل كارت: المستلم + Badge حالة (مصروفة/بانتظار/مرفوضة) + meta (كمية، منفذ، تاريخ، سبب)
- قسم إدارة (إن كان لديه صلاحية إضافة): زر "تعديل بيانات الأداة" + زر "حذف الأداة" (للمدير فقط)

### 6. إضافة / تعديل أداة `tool/new.tsx`
**المسار:** `/tool/new` و `/tool/new?editId=XXX` و `/tool/new?barcode=YYY`
**ما يظهر:**
- قسم صورة: معاينة 110×110 + 3 أزرار: التقاط صورة (كاميرا), اختيار من المعرض, إزالة
- حقل "اسم الأداة" * مطلوب
- حقل "الرقم التسلسلي"
- حقل "الباركود / QR" + زر مسح يفتح شاشة scan
- قسم التصنيف: chips ملونة (اختيار واحد), يمكن إلغاء الاختيار
- حقل الموقع
- صفين: الكمية الإجمالية * + حد التنبيه (hint: تنبيه عند الوصول)
- حقل وصف multiline
- حقل ملاحظات multiline
- زر حفظ: "إضافة الأداة" أو "حفظ التعديلات"

### 7. مسح الباركود `scan.tsx`
**المسار:** `/scan?returnTo=form|withdraw`
**ما يظهر:**
- طلب إذن كاميرا (شاشة إذن بها شرح)
- كاميرا ملء الشاشة تدعم 10 أنواع باركود: qr, ean13, code128...
- Overlay توجيهي: إطار مع 4 زوايا + نص "وجه الكاميرا نحو الباركود"
- أزرار سفلية: إضاءة فلاش (torch) + إغلاق
- المنطق: عند المسح يبحث findToolByCode، إن وجد يذهب للتفاصيل أو لشاشة الصرف، إن لم يجد يعرض Alert "رمز غير مسجل" مع خيار "إنشاء أداة بهذا الرمز"

### 8. تسجيل صرف جديد `withdraw/new.tsx`
**المسار:** `/withdraw/new` و `/withdraw/new?toolId=XXX`
**ما يظهر:**
- حقل اختيار الأداة *: Selector يفتح Sheet
- زر "أو امسح الباركود"
- حقل كمية * (number-pad) مع hint "الحد الأقصى: X"
- حقل اسم المستلم * (الجهة الطالبة)
- chips سبب الصرف: صيانة, مهمة ميدانية, استبدال, تركيب, أخرى
- حقل ملاحظات
- تحذير أصفر إن لم يكن لديه صلاحية صرف مباشر: "سيُسجل بانتظار موافقة المدير"
- زر "تسجيل الصرف" أو "إرسال طلب الصرف"
- Sheet اختيار أداة: بحث + قائمة أدوات متاحة فقط + trống

### 9. تسجيل إضافة كمية `addition/new.tsx`
**المسار:** `/addition/new` و `/addition/new?toolId=XXX`
**ما يظهر:**
- Selector أداة *
- رابط "الأداة غير موجودة؟ أضف أداة جديدة"
- حقل كمية مضافة * 
- chips مصدر الإضافة: شراء, تبرع, إرجاع, تحويل, أخرى
- ملاحظات (رقم فاتورة...)
- معاينة خضراء: "بعد الإضافة سيصبح الإجمالي: X وحدة"
- زر "تسجيل الإضافة" أخضر

### 10. تبويب الصرف - قائمة عمليات الصرف `disbursements.tsx`
**المسار:** `/(tabs)/disbursements?filter=pending|approved`
**ما يظهر:**
- بحث: باسم الأداة أو المستلم
- فلاتر chips: الكل, بانتظار الموافقة, مصروفة, مرفوضة
- قائمة كروت، كل كارت:
  - اسم الأداة + Badge حالة ملون (pending=أصفر, approved=أخضر, rejected=رمادي)
  - meta: كمية, مستلم, منفذ, تاريخ
  - إن كانت pending وللمستخدم صلاحية موافقة: زرين "رفض" (outline) + "موافقة"
- EmptyState إن لا يوجد

### 11. تبويب التقارير `reports.tsx`
**المسار:** `/(tabs)/reports`
**ما يظهر:**
- كارت ملخص: عدد الأصناف, الوحدات, المتاح, مسحوب, كمية منخفضة
- كارت فترة التقرير: حقلين تاريخ من/إلى (YYYY-MM-DD)
- قسم "تقارير PDF":
  - جرد المخزن الكامل (Item: albums-outline)
  - تقرير الصرف (من الفترة) (arrow-down-circle)
  - أكثر الأدوات صرفاً (trophy)
  - كل Item يظهر loading hourglass عند التصدير
- قسم "تصدير Excel":
  - تصدير الجرد (xlsx)
  - تصدير الحركات (xlsx) ورقتين: الصرف + الإضافات
- قسم "الأكثر صرفاً" (إن وجد): قائمة مرقمة مع rank دائري + اسم + عدد مرات
- تذييل: "كل التقارير تولد محلياً"

### 12. تبويب الإعدادات `settings.tsx`
**المسار:** `/(tabs)/settings`
**ما يظهر:**
- كارت حساب: Avatar + اسم كامل + username + دور
- قسم البيانات والنسخ الاحتياطي:
  - جدول الأصناف (يذهب لـ tools_table)
  - استيراد من Excel
  - تنزيل قالب Excel
  - نسخة احتياطية (يظهر progress % أثناء الضغط، يستخدم backup JSON)
  - استعادة نسخة (خطر، أحمر)
- قسم تخصيص جدول الأصناف:
  - Switch: إظهار عمود التصنيف
  - زرين فرز افتراضي: الاسم / الكمية (يضيء المختار)
- قسم الإدارة (للمدير فقط):
  - إدارة المستخدمين
  - إدارة التصنيفات
  - سجل الإجراءات
- قسم التنبيهات المحلية:
  - فحص الأدوات المتأخرة -> Alert بعدد المتأخرة
  - فحص الكميات المنخفضة -> Alert
- قسم معلومات النظام (Card مع InfoRows):
  - إصدار التطبيق, حجم DB, الجهاز, أندرويد, المساحة المتاحة, الوحدة Kotlin, نوع البناء, المستخدم الحالي
- أزرار سفلية: تغيير كلمة المرور (outline) + تسجيل خروج (danger)
- Sheet تغيير كلمة مرور: حقلين + حفظ

### 13. إدارة المستخدمين `users.tsx`
**المسار:** `/users` (مدير فقط)
**ما يظهر:**
- قائمة كروت مستخدمين (من Firestore onSnapshot):
  - اسم كامل + username + "(أنت)" إن هو
  - Badge دور (مدير=أحمر, مستخدم=أزرق)
  - Badges صلاحيات: صرف مباشر (أخضر), إضافة أدوات (أزرق), معطل (رمادي)
- زر سفلي ثابت "إضافة مستخدم"
- Sheet مستخدم جديد:
  - اسم مستخدم إنجليزي * (validation regex)
  - اسم كامل *
  - كلمة مرور * (6+)
  - Switch مدير مخزن؟
  - Switch صرف مباشر؟ (مع hint)
  - Switch إضافة أدوات؟
  - حفظ

### 14. إدارة التصنيفات `categories.tsx`
**المسار:** `/categories`
**ما يظهر:**
- قائمة كروت: نقطة ملونة (dot) + اسم + عدد الأدوات المرتبطة + زر حذف Trash
- FAB +
- Sheet تصنيف جديد: اسم * + اختيار لون من 8 ألوان Palette (مع checkmark) + إضافة

### 15. سجل الإجراءات `audit.tsx`
**المسار:** `/audit`
**ما يظهر:**
- FlatList سجلات من audit_logs ordered desc
- كل صف: أيقونة ملونة حسب الحدث (login=info, create=success, delete=danger...) + خلفية فاتحة
- نص: label عربي (إنشاء, تعديل, سحب, موافقة...) + details إن وجد
- meta: actor • entity#id • تاريخ
- EmptyState

### 16. استيراد Excel `import-excel.tsx`
**المسار:** `/import-excel`
**ما يظهر:**
- كارت أعمدة مطلوبة: A=الاسم مطلوب, B=تسلسلي اختياري, C=تصنيف..., D=موقع, E=كمية, F=ملاحظات
- زر "تنزيل قالب جاهز"
- زر "اختيار ملف Excel"
- بعد اختيار:
  - كارت ملف: اسم + عدد صفوف صالحة (أخضر) + عدد به خطأ (أحمر)
  - كارت أحمر إن في invalid: أول 8 أخطاء "الصف X: ..."
  - معاينة أول 10 صفوف صالحة: اسم + meta + Badge كمية
  - زر "بدء استيراد X أداة"
- تذييل: "تتم قراءة الملف على الجهاز فقط"

### 17. واجهات مشتركة (UI Components)
- **Button:** 5 أنواع (primary, outline, danger, ghost, success) + أيقونة + loading spinner
- **Field:** label + * + input rounded + hint
- **Card:** خلفية بيضاء rounded 16 + shadow خفيف
- **Badge:** pill ملون حسب tone
- **EmptyState:** أيقونة كبيرة + عنوان + وصف
- **Loader:** spinner + نص
- **Sheet:** Modal منزلق من الأسفل مع header + زر إغلاق
- **InfoRow:** صف أيقونة + label + value

### ملاحظات التصميم
- كل الواجهات RTL (forceRTL)
- ألوان ثابتة من src/theme: primary #0F766E, bg #F1F5F9, card #FFF, border #E2E8F0
- تبويب سفلي 62px, خلفية بيضاء, Active أخضر
- كل القوائم تستخدم FlatList مع paddingBottom 90 لتجنب FAB
- كل شاشة تدعم Pull-to-Refresh حيث مناسب

هذه هي جميع الواجهات التي يراها المستخدم النهائي في التطبيق بعد الإصلاح الكامل (TSC 0 errors).
